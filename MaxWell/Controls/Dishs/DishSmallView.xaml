<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="MaxWell.Controls.Dishs.DishSmallView"
             >
     <ContentView.Content>
        <StackLayout Margin="10,0,5,0" WidthRequest="64" HeightRequest="64">
            <forms:CachedImage HorizontalOptions="Start" Source="{Binding ImageUrl, Mode=OneWay}"
                   WidthRequest="64" HeightRequest="64" />
            <Label HorizontalTextAlignment="Center" Margin="0,-5,0,0"
                   LineBreakMode="TailTruncation" FontSize="12" Text="{Binding Name}"
                   TextColor="White" />
        </StackLayout>
    </ContentView.Content>
</ContentView>